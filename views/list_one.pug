extends layout

block content
    main
      form(action="/new_task")
        button Add a new maintenance task
      div.card
        div.cardcontainer
          h2
            b= report.datetime
          p Reported by: 
            b= report.reporter
          p Affected location(s):
            b= report.where
          p Comment:
            b= report.task
